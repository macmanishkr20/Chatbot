"""
Prompts for the synthesize node (Phase 2 — Autonomous Multi-Step Agent).

NOTE: The synthesize node is currently purely programmatic (no LLM call).
These prompts are reserved for future use if LLM-based synthesis is needed
for complex cross-function answer merging.
"""

# Reserved for future LLM-based synthesis if programmatic merging proves insufficient.
# Currently synthesize_node interleaves events by function without calling an LLM.
