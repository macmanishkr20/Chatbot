# MenaBot — M365 Agents SDK + LangGraph RAG Pipeline
# Dependencies

# ── Core Framework ──
fastapi
uvicorn[standard]
python-dotenv
pydantic>=2.0

# ── LangGraph / LangChain ──
langgraph>=0.2.28
langchain-core>=0.3.0
langchain-openai>=0.2.0
langgraph-checkpoint>=2.0.0

# ── M365 Agents SDK ──
# Note: microsoft-agents-core, microsoft-agents-builder, microsoft-agents-authorization,
# and microsoft-agents-connector do not exist on PyPI. The hosting package includes all needed modules.
microsoft-agents-hosting-aiohttp>=0.8.0

# ── Azure AI / OpenAI ──
openai>=1.0.0
azure-identity
azure-search-documents>=11.4.0
azure-core
azure-keyvault-secrets
tiktoken

# ── Database ──
pyodbc
sqlalchemy
aioodbc
aiosqlite

# ── Utilities ──
tenacity
aiohttp
slowapi

# ── Optional: Observability ──
# langfuse
# microsoft-agents-a365-observability-extensions-langchain
# azure-monitor-opentelemetry

# ── Optional: Screen-share + voice assistance (feature/screen-share-voice) ──
# Uncomment (and set AZURE_OPENAI_REALTIME_* env vars) to enable the
# /ws/screenshare/* endpoints. The module is loaded lazily in app.py so
# absence of these packages does not break the main chat service.
# aiortc>=1.9.0
# av>=12.3.0
# Pillow>=10.4.0
# imagehash>=4.3.1
# websockets>=13.0
# azure-cognitiveservices-speech>=1.40.0
